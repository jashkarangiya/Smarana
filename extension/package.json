{
  "name": "smarana-extension",
  "version": "0.1.0",
  "description": "Smarana browser extension for spaced repetition review of coding problems",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development & vite build -c vite.content.config.ts --watch --mode development",
    "build": "vite build && vite build -c vite.content.config.ts",
    "zip": "cd dist && zip -r ../smarana-extension.zip .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "sharp": "^0.34.5",
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  }
}
